"use strict";
exports.__esModule = true;
var replace_head_1 = require("./replace-head");
var Init = /** @class */ (function () {
    function Init() {
        var replaceHead = new replace_head_1.ReplaceHead();
        replaceHead.init();
    }
    return Init;
}());
exports.Init = Init;
new Init();
